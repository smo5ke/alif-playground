استورد عشوائي

صنف عشوائية(عشوائي.عشوائية):
		دالة __تهيئة__(هذا, س=عدم):
			هذا.البذرة(س)
		
		دالة البذرة(هذا, س=عدم):
			اصل().بذرة(س)
	
		دالة منتظم(هذا, س, ص):
			ارجع س + (ص - س) * هذا.عشوائي()

_نسخ = عشوائية()
البذرة = _نسخ.البذرة
عشوائي = _نسخ.عشوائي
منتظم = _نسخ.منتظم